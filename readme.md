# Remote AI

Scripts to manage working on codebases on a remote vm running ai/llm tools, if you don't trust them to run on your own machine. Assumes hetzner cloud.

To install, add `export PATH="$HOME/rai:$PATH"` to your `~/.bashrc`.
