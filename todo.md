# TODO for rai

- [] sshing to the machine fails if the project doesn't exist there yet. That's not ideal.
- [] rai-cp should also be able to handle full paths outside of git repositories (for example for ssh-key uploads).
- [] rai should be independent of cloud provider - in time I want to run it on my own physical machine with some hypervisor set up.
- [] rai-push doesn't switch the current branch. It should.
- [] should save things to files in the repo (like username, remote machine name, etc.), and also allow overrides in the home folder.
