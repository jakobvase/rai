# TODO for rai

- [] When the server is restarted, and the volume is encrypted, `rai-ssh` fails and `rai-unlock` fails too. `rai-ssh` fails with a "mkdir - access denied" or something similar, and `rai-unlock` fails with "sudo: Sorry, I can't do that" (I'm assuming it's the mount).
