#!/bin/bash
# rai - Remote AI workspace manager
set -euo pipefail

SCRIPT_DIR="$(dirname "$(realpath "$0")")"

COMMAND="${1:-}"

case "$COMMAND" in
  start|stop|push|pull|cp|ssh|code|unlock)
    shift
    "$SCRIPT_DIR/rai-$COMMAND" "$@"
    ;;
  *)
    echo "Usage: rai <start|stop|push|pull|cp|ssh|code|unlock>"
    exit 1
    ;;
esac